[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "knarnaslibrary"
authors = [{name = "Jacob Noel-Storr", email = "jake@accessastronomy.eu"}]
dynamic = ["version", "description"]
